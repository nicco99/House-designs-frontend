/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.inuua.net", "www.shutterstock.com", "cdni.iconscout.com"],
  },
};

module.exports = nextConfig;
